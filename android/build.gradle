// allprojects {
//     repositories {
//         google()
//         mavenCentral()
//     }
// }

// rootProject.buildDir = "../build"
// subprojects {
//     project.buildDir = "${rootProject.buildDir}/${project.name}"
// }
// subprojects {
//     project.evaluationDependsOn(":app")
// }

// tasks.register("clean", Delete) {
//     delete rootProject.buildDir
// }

// Top-level build file where you can add configuration options common to all sub-projects/modules.
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // إضافة Firebase BoM
        classpath 'com.google.gms:google-services:4.3.15' // تأكد من تحديث النسخة إلى أحدث إصدار
        //classpath 'com.android.tools.build:gradle:8.0.0' // تأكد من تحديث النسخة إذا لزم الأمر
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

